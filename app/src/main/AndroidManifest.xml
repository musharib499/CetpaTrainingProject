<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".demoapp.ui.CetpaApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CetpaTrainingProject"
        tools:targetApi="31">
        <activity
            android:name=".day14.ListViewActivity"
            android:exported="true" >
        </activity>
        <activity
            android:name=".day14.GridActivity"
            android:exported="true">
        </activity>
        <activity
            android:name=".demoapp.ui.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".demoapp.ui.SplashActivity"
            android:exported="true">

             <intent-filter>
             <action android:name="android.intent.action.MAIN" />


             <category android:name="android.intent.category.LAUNCHER" />
             </intent-filter>
        </activity>
    </application>

</manifest>